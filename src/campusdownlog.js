// src/CampusDownLogo.js

import { motion } from "framer-motion";

export default function CampusDownLogo() {
  // ...[여기에 아까 준 CampusDownLogo 코드 복사]...
  // (길어서 줄임, 위에서 준 전체 코드 붙이면 됨)
  return (
    <div style={{ fontWeight: "bold", fontSize: 48, color: "#222" }}>
      Campus<span style={{ color: "#1a73e8" }}>Down</span>
    </div>
  );
}
